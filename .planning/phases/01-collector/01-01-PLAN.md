---
phase: 01-collector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent-network/package.json
  - agent-network/server/services/news-collector/config.json
  - agent-network/server/services/news-collector/storage/db.js
  - agent-network/.gitignore
autonomous: true
requirements:
  - COLL-03
  - COLL-04

must_haves:
  truths:
    - "Running `npm install` in agent-network completes without errors after rss-parser and better-sqlite3 are added"
    - "config.json exists and contains all 10 sources, each with id/name/type/enabled fields"
    - "Calling initDb() creates news.db with seen_urls table and idx_seen_at index"
    - "isSeen() returns false for a URL never seen; markSeen() inserts it; isSeen() then returns true"
    - "news.db is gitignored"
  artifacts:
    - path: "agent-network/server/services/news-collector/config.json"
      provides: "Source list with 10 entries driven by COLL-04"
      contains: '"pollIntervalCron": "0 */6 * * *"'
    - path: "agent-network/server/services/news-collector/storage/db.js"
      provides: "SQLite deduplication layer"
      exports: ["initDb", "isSeen", "markSeen"]
  key_links:
    - from: "config.json"
      to: "collector orchestrator (plan 04)"
      via: "import config from './config.json' assert { type: 'json' }"
      pattern: '"id":'
    - from: "storage/db.js"
      to: "collector orchestrator (plan 04)"
      via: "isSeen + markSeen calls"
      pattern: "export function isSeen|export function markSeen"
---

<objective>
Bootstrap the news-collector module: install the two new npm dependencies, create the directory scaffold, write the source config file (COLL-04), and implement the SQLite deduplication layer (COLL-03).

Purpose: All subsequent plans depend on the packages being installed, the config schema being established, and the storage API being available. This plan creates the foundation everything else builds on.
Output: agent-network/server/services/news-collector/ directory with config.json and storage/db.js; rss-parser and better-sqlite3 installed.
</objective>

<execution_context>
@C:/Users/Herba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Herba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@d:/_PAL/benino/.planning/PROJECT.md
@d:/_PAL/benino/.planning/ROADMAP.md
@d:/_PAL/benino/.planning/phases/01-collector/01-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor needs. -->

From agent-network/server/utils/expired-listings-cron.js (pattern reference):
```javascript
import cron from 'node-cron';
cron.schedule('0 */6 * * *', async () => { ... });
```

From agent-network/package.json (type: "module" — ESM only, NO require()):
```json
{ "type": "module" }
```

better-sqlite3 synchronous API (no callbacks, no promises):
```javascript
import Database from 'better-sqlite3';
const db = new Database('/path/to/news.db');
const row = db.prepare('SELECT 1 FROM seen_urls WHERE url_hash = ?').get(hash);
db.prepare('INSERT OR IGNORE INTO seen_urls ...').run(...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create directory scaffold</name>
  <files>agent-network/package.json, agent-network/.gitignore</files>
  <read_first>
    - d:/_PAL/benino/agent-network/package.json (current deps — confirm rss-parser and better-sqlite3 are absent before adding)
    - d:/_PAL/benino/agent-network/.gitignore (find where to insert news.db ignore)
  </read_first>
  <action>
1. From `d:/_PAL/benino/agent-network/` run:
   ```bash
   npm install rss-parser@3.13.0 better-sqlite3@12.8.0
   ```
   This updates package.json and package-lock.json automatically.

2. Create the directory structure (these are empty dirs, just ensure they exist — files will be created in later tasks):
   ```
   agent-network/server/services/news-collector/
   agent-network/server/services/news-collector/sources/
   agent-network/server/services/news-collector/storage/
   ```
   Create a placeholder `.gitkeep` in the empty `sources/` dir only if needed to track it. DO NOT create any JS source files yet.

3. Add to `agent-network/.gitignore` (append after existing entries):
   ```
   # News collector SQLite database
   server/services/news-collector/news.db
   ```
  </action>
  <verify>
    <automated>cd d:/_PAL/benino/agent-network && node -e "import('rss-parser').then(m => console.log('rss-parser OK:', typeof m.default))" && node -e "import('better-sqlite3').then(m => console.log('better-sqlite3 OK:', typeof m.default))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "rss-parser" d:/_PAL/benino/agent-network/package.json` outputs a line with `"rss-parser"`
    - `grep "better-sqlite3" d:/_PAL/benino/agent-network/package.json` outputs a line with `"better-sqlite3"`
    - `ls d:/_PAL/benino/agent-network/server/services/news-collector/sources/` does not error
    - `ls d:/_PAL/benino/agent-network/server/services/news-collector/storage/` does not error
    - `grep "news.db" d:/_PAL/benino/agent-network/.gitignore` outputs the ignore line
    - Both node ESM import commands in verify succeed with "OK" output
  </acceptance_criteria>
  <done>rss-parser and better-sqlite3 installed; directory scaffold exists; news.db gitignored</done>
</task>

<task type="auto">
  <name>Task 2: Write config.json with all 10 sources (COLL-04)</name>
  <files>agent-network/server/services/news-collector/config.json</files>
  <read_first>
    - d:/_PAL/benino/.planning/phases/01-collector/01-RESEARCH.md (Pattern 1: Source Config Schema — copy the full config.json from there, then apply corrections below)
  </read_first>
  <action>
Create `d:/_PAL/benino/agent-network/server/services/news-collector/config.json` with the following EXACT content. These values come directly from verified research. Apply these corrections vs the research draft:

- TAAFT source: change `"type": "rss"` to `"type": "taaft-scrape"` and `"url"` to `"https://theresanaiforthat.com/recently-added/"` (the RSS URL is broken — see research Pitfall 1)
- HuggingFace Papers source: change `"type": "rss"` and HF blog URL to `"type": "hf-papers-api"` (use the official API, not the blog RSS — more reliable)
- Add `"maxItemsPerSource": 30` at the top level

Final config.json:
```json
{
  "pollIntervalCron": "0 */6 * * *",
  "maxItemsPerSource": 30,
  "sources": [
    {
      "id": "anthropic",
      "name": "Anthropic Blog",
      "type": "rss",
      "url": "https://raw.githubusercontent.com/taobojlen/anthropic-rss-feed/main/anthropic_news_rss.xml",
      "enabled": true
    },
    {
      "id": "openai",
      "name": "OpenAI Blog",
      "type": "rss",
      "url": "https://openai.com/news/rss.xml",
      "enabled": true
    },
    {
      "id": "google-ai",
      "name": "Google AI Blog",
      "type": "rss",
      "url": "https://blog.google/technology/ai/rss/",
      "enabled": true
    },
    {
      "id": "github-changelog",
      "name": "GitHub Blog Changelog",
      "type": "rss",
      "url": "https://github.blog/changelog/feed/",
      "enabled": true
    },
    {
      "id": "huggingface-papers",
      "name": "HuggingFace Daily Papers",
      "type": "hf-papers-api",
      "limit": 20,
      "enabled": true
    },
    {
      "id": "taaft",
      "name": "TAAFT - There's An AI For That",
      "type": "taaft-scrape",
      "url": "https://theresanaiforthat.com/recently-added/",
      "enabled": true
    },
    {
      "id": "hackernews",
      "name": "HackerNews",
      "type": "hn-algolia",
      "query": "AI OR LLM OR GPT OR Claude OR Gemini",
      "minPoints": 10,
      "hitsPerPage": 30,
      "enabled": true
    },
    {
      "id": "github-trending",
      "name": "GitHub Trending",
      "type": "github-trending",
      "language": "",
      "since": "daily",
      "enabled": true
    },
    {
      "id": "github-search",
      "name": "GitHub Search AI Models",
      "type": "github-search",
      "query": "topic:large-language-model pushed:>DATE",
      "sort": "updated",
      "perPage": 20,
      "enabled": true
    },
    {
      "id": "huggingface-models",
      "name": "HuggingFace Models",
      "type": "hf-models-api",
      "filter": "text-generation",
      "limit": 20,
      "enabled": true
    }
  ]
}
```
  </action>
  <verify>
    <automated>cd d:/_PAL/benino/agent-network && node -e "import('./server/services/news-collector/config.json', {assert:{type:'json'}}).then(m => { const c = m.default; console.log('sources:', c.sources.length, 'cron:', c.pollIntervalCron); })"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `ls d:/_PAL/benino/agent-network/server/services/news-collector/config.json` exits 0
    - `grep '"pollIntervalCron": "0 \*/6 \* \* \*"' d:/_PAL/benino/agent-network/server/services/news-collector/config.json` matches
    - The node import command outputs `sources: 10`
    - `grep '"type": "taaft-scrape"' d:/_PAL/benino/agent-network/server/services/news-collector/config.json` matches (confirms the TAAFT fix is applied)
    - `grep '"type": "hf-papers-api"' d:/_PAL/benino/agent-network/server/services/news-collector/config.json` matches
    - All 10 source `id` values are unique: anthropic, openai, google-ai, github-changelog, huggingface-papers, taaft, hackernews, github-trending, github-search, huggingface-models
  </acceptance_criteria>
  <done>config.json has 10 sources with correct types and URLs; cron expression is "0 */6 * * *"; TAAFT and HF Papers use correct types</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement SQLite deduplication layer (COLL-03)</name>
  <files>agent-network/server/services/news-collector/storage/db.js</files>
  <read_first>
    - d:/_PAL/benino/.planning/phases/01-collector/01-RESEARCH.md (Pattern 3: SQLite Deduplication — copy the full db.js from there exactly)
  </read_first>
  <behavior>
    - initDb() on a fresh path → creates news.db, creates seen_urls table with url_hash PRIMARY KEY, url, title, source_id, seen_at columns, creates idx_seen_at index
    - isSeen(db, 'https://example.com/test') → returns false (never seen)
    - markSeen(db, 'https://example.com/test', 'Test Title', 'openai') → inserts row
    - isSeen(db, 'https://example.com/test') → returns true (now seen)
    - isSeen(db, 'https://example.com/test/') → returns true (trailing slash normalized)
    - markSeen(db, same url, ...) called twice → no error (INSERT OR IGNORE), count stays 1
    - hashUrl normalizes: trim + strip trailing slash, then SHA-256 hex, take first 32 chars
  </behavior>
  <action>
Create `d:/_PAL/benino/agent-network/server/services/news-collector/storage/db.js` with EXACTLY this content (copied from research Pattern 3 with no modifications):

```javascript
import Database from 'better-sqlite3';
import crypto from 'crypto';
import path from 'path';
import { fileURLToPath } from 'url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const DB_PATH = path.join(__dirname, '..', 'news.db');

export function initDb() {
  const db = new Database(DB_PATH);
  db.exec(`
    CREATE TABLE IF NOT EXISTS seen_urls (
      url_hash  TEXT PRIMARY KEY,
      url       TEXT NOT NULL,
      title     TEXT,
      source_id TEXT,
      seen_at   INTEGER NOT NULL
    );
    CREATE INDEX IF NOT EXISTS idx_seen_at ON seen_urls(seen_at);
  `);
  return db;
}

export function isSeen(db, url) {
  const hash = hashUrl(url);
  return !!db.prepare('SELECT 1 FROM seen_urls WHERE url_hash = ?').get(hash);
}

export function markSeen(db, url, title, sourceId) {
  const hash = hashUrl(url);
  db.prepare(`
    INSERT OR IGNORE INTO seen_urls (url_hash, url, title, source_id, seen_at)
    VALUES (?, ?, ?, ?, ?)
  `).run(hash, url, title, sourceId, Date.now());
}

function hashUrl(url) {
  const normalized = url.trim().replace(/\/$/, '');
  return crypto.createHash('sha256').update(normalized).digest('hex').substring(0, 32);
}
```

After writing the file, run this inline smoke test:
```bash
cd d:/_PAL/benino/agent-network && node -e "
import('./server/services/news-collector/storage/db.js').then(({ initDb, isSeen, markSeen }) => {
  const db = initDb();
  const url = 'https://example.com/test-dedup';
  console.assert(!isSeen(db, url), 'Should not be seen yet');
  markSeen(db, url, 'Test', 'test-source');
  console.assert(isSeen(db, url), 'Should be seen now');
  console.assert(isSeen(db, url + '/'), 'Trailing slash should match');
  markSeen(db, url, 'Test', 'test-source'); // idempotent
  const count = db.prepare('SELECT COUNT(*) as c FROM seen_urls WHERE url = ?').get(url);
  console.assert(count.c === 1, 'Should have exactly 1 row');
  db.close();
  console.log('db.js smoke test: PASSED');
});
"
```
The test must print "PASSED" before considering this task done.
  </action>
  <verify>
    <automated>cd d:/_PAL/benino/agent-network && node -e "import('./server/services/news-collector/storage/db.js').then(({initDb,isSeen,markSeen}) => { const db=initDb(); markSeen(db,'https://t.co/x','T','s'); console.log(isSeen(db,'https://t.co/x') ? 'PASS' : 'FAIL'); db.close(); })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export function initDb" d:/_PAL/benino/agent-network/server/services/news-collector/storage/db.js` matches
    - `grep "export function isSeen" d:/_PAL/benino/agent-network/server/services/news-collector/storage/db.js` matches
    - `grep "export function markSeen" d:/_PAL/benino/agent-network/server/services/news-collector/storage/db.js` matches
    - `grep "INSERT OR IGNORE" d:/_PAL/benino/agent-network/server/services/news-collector/storage/db.js` matches (idempotency)
    - `grep "substring(0, 32)" d:/_PAL/benino/agent-network/server/services/news-collector/storage/db.js` matches (32-char hash)
    - The verify command outputs "PASS"
    - The inline smoke test at end of action outputs "db.js smoke test: PASSED"
  </acceptance_criteria>
  <done>storage/db.js exported with initDb/isSeen/markSeen; dedup logic verified with round-trip test and trailing-slash normalization confirmed</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `cd d:/_PAL/benino/agent-network && node -e "import('rss-parser').then(() => import('better-sqlite3')).then(() => console.log('deps OK'))"` — prints "deps OK"
2. `node -e "import('./server/services/news-collector/config.json', {assert:{type:'json'}}).then(m => console.log(m.default.sources.length === 10 ? 'config OK' : 'FAIL'))"` — prints "config OK"
3. `node -e "import('./server/services/news-collector/storage/db.js').then(({initDb,isSeen,markSeen}) => { const db=initDb(); markSeen(db,'https://x.com','t','s'); console.log(isSeen(db,'https://x.com') ? 'storage OK' : 'FAIL'); })"` — prints "storage OK"
</verification>

<success_criteria>
- rss-parser@3.13.0 and better-sqlite3@12.8.0 appear in agent-network/package.json
- config.json has exactly 10 sources with types: rss(×4), hf-papers-api(×1), taaft-scrape(×1), hn-algolia(×1), github-trending(×1), github-search(×1), hf-models-api(×1)
- storage/db.js exports initDb, isSeen, markSeen; round-trip dedup test passes
- news.db is gitignored
</success_criteria>

<output>
After completion, create `d:/_PAL/benino/.planning/phases/01-collector/01-01-SUMMARY.md`
</output>
