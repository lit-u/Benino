---
phase: 08-rezervacij-kalendorius
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - agent-network/tests/hotel-calendar.spec.js
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-03
  - CAL-04
  - CAL-05
  - CAL-06

must_haves:
  truths:
    - "Test file exists with 6 test.skip() stubs covering CAL-01 through CAL-06"
    - "Test file compiles without server dependency"
  artifacts:
    - path: "agent-network/tests/hotel-calendar.spec.js"
      provides: "Executable spec stubs for calendar features"
      contains: "test.skip"
  key_links: []
---

<objective>
Create Playwright test stubs for the reservation calendar feature (CAL-01 through CAL-06).

Purpose: Establish executable spec before implementation — same Wave-0 pattern used in Phases 05, 06, 07.
Output: `agent-network/tests/hotel-calendar.spec.js` with 6 `test.skip()` stubs.
</objective>

<execution_context>
@C:/Users/Herba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Herba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rezervacij-kalendorius-vizualus-kalendorius-admin-panelje-ir-dat-parinkiklis-vie-ajame-puslapyje/08-CONTEXT.md
@agent-network/tests/hotel-reservations.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hotel-calendar.spec.js with 6 test.skip() stubs</name>
  <files>agent-network/tests/hotel-calendar.spec.js</files>
  <read_first>
    - agent-network/tests/hotel-reservations.spec.js
    - agent-network/tests/hotel-multi.spec.js
  </read_first>
  <action>
Create `agent-network/tests/hotel-calendar.spec.js` following the exact pattern from `hotel-reservations.spec.js` and `hotel-multi.spec.js`. Use `import { test, expect } from '@playwright/test';` at the top.

Create a `test.describe('Hotel Calendar', () => { ... })` block with exactly 6 `test.skip()` stubs:

1. **CAL-01** — `'e2e-admin-calendar: admin panel shows monthly calendar grid with reservation entries'`
   - Comment: `// Requires: Plan 08-01 (admin calendar tab)`
   - Navigate to `/hotel/test-hotel/admin`
   - Assert `page.locator('.calendar-grid')` is visible
   - Assert `page.locator('.calendar-chip')` count is greater than 0

2. **CAL-02** — `'e2e-admin-calendar-colors: calendar entries are color-coded by status'`
   - Comment: `// Requires: Plan 08-01 (calendar color coding)`
   - Navigate to `/hotel/test-hotel/admin`
   - Assert `page.locator('.calendar-chip.status-confirmed')` has CSS `background-color` containing `34, 197, 94` (green)
   - Assert `page.locator('.calendar-chip.status-pending')` has CSS `background-color` containing `245, 158, 11` (amber)

3. **CAL-03** — `'e2e-admin-calendar-click: clicking calendar entry opens reservation detail'`
   - Comment: `// Requires: Plan 08-01 (calendar click interaction)`
   - Navigate to `/hotel/test-hotel/admin`
   - Click `.calendar-chip`
   - Assert `.reservation-detail` or `.reservation-card` is visible

4. **CAL-04** — `'e2e-public-datepicker: public hotel page has check-in/check-out date inputs'`
   - Comment: `// Requires: Plan 08-02 (date picker min attributes)`
   - Navigate to `/hotel/test-hotel`
   - Assert arrival date input has `min` attribute equal to today's date (ISO format)

5. **CAL-05** — `'e2e-public-datepicker-validation: date pickers block past dates and enforce checkout > checkin'`
   - Comment: `// Requires: Plan 08-02 (date picker validation)`
   - Navigate to `/hotel/test-hotel`
   - Set arrival input value
   - Assert departure input `min` attribute equals arrival + 1 day

6. **CAL-06** — `'e2e-public-datepicker-submit: selected dates submitted in reservation POST body'`
   - Comment: `// Requires: Plan 08-02 (date picker form integration)`
   - Navigate to `/hotel/test-hotel`
   - Intercept POST to `/api/hotels/*/reservations`
   - Assert request body contains `arrival_date` and `departure_date`

All stubs use `test.skip(...)` so they compile but do not execute.
  </action>
  <verify>
    <automated>cd d:/_PAL/benino/agent-network && npx playwright test hotel-calendar.spec.js --list 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `agent-network/tests/hotel-calendar.spec.js`
    - File contains exactly 6 `test.skip(` calls
    - File contains strings: `CAL-01`, `CAL-02`, `CAL-03`, `CAL-04`, `CAL-05`, `CAL-06`
    - File contains `import { test, expect } from '@playwright/test'`
    - File contains `test.describe('Hotel Calendar'`
    - `npx playwright test hotel-calendar.spec.js --list` succeeds without errors
  </acceptance_criteria>
  <done>6 test.skip() stubs exist in hotel-calendar.spec.js, one per CAL requirement, file lists without errors</done>
</task>

</tasks>

<verification>
- `npx playwright test hotel-calendar.spec.js --list` shows 6 tests, all skipped
- grep confirms all 6 CAL-XX IDs present
</verification>

<success_criteria>
hotel-calendar.spec.js exists with 6 test.skip() stubs covering CAL-01 through CAL-06
</success_criteria>

<output>
After completion, create `.planning/phases/08-rezervacij-kalendorius-vizualus-kalendorius-admin-panelje-ir-dat-parinkiklis-vie-ajame-puslapyje/08-00-SUMMARY.md`
</output>
