---
phase: 06-rezervacij-sistema
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - agent-network/tests/hotel-reservations.spec.js
autonomous: true
requirements:
  - RES-01
  - RES-02
  - RES-03
  - RES-04

must_haves:
  truths:
    - "Playwright test file exists with test.skip() stubs for all reservation behaviors"
    - "Running npx playwright test hotel-reservations.spec.js compiles without error (all tests skipped)"
  artifacts:
    - path: "agent-network/tests/hotel-reservations.spec.js"
      provides: "Test stubs for reservation API and E2E behaviors"
      min_lines: 40
  key_links: []
---

<objective>
Create Playwright test stubs (test.skip) for all reservation system behaviors before any backend code is written.

Purpose: Wave 0 pattern established in Phase 5 — test stubs compile without server dependency and document expected behaviors upfront.
Output: hotel-reservations.spec.js with skipped tests covering POST/GET/PATCH API + frontend form + admin panel.
</objective>

<execution_context>
@C:/Users/Herba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Herba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-rezervacij-sistema-tikros-rezervacijos-db-savininkas-gauna-el-lai-k-ir-whatsapp-prane-im-apie-kiekvien-rezervacij/06-CONTEXT.md
@.planning/phases/06-rezervacij-sistema-tikros-rezervacijos-db-savininkas-gauna-el-lai-k-ir-whatsapp-prane-im-apie-kiekvien-rezervacij/06-RESEARCH.md

<interfaces>
<!-- Existing test pattern from hotel.spec.js -->
From agent-network/tests/hotel.spec.js:
```javascript
import { test, expect } from '@playwright/test';
test.describe('Hotel Mini-Website', () => {
  test.skip('ssr-title: /hotel/:slug returns HTML with hotel name in title', async ({ page }) => {
    // ...
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hotel-reservations.spec.js with test.skip() stubs</name>
  <files>agent-network/tests/hotel-reservations.spec.js</files>
  <read_first>
    - agent-network/tests/hotel.spec.js (existing test pattern — test.skip() + describe structure)
    - agent-network/playwright.config.cjs (test config — baseURL, projects)
  </read_first>
  <action>
Create `agent-network/tests/hotel-reservations.spec.js` with the following test.skip() stubs inside a `test.describe('Hotel Reservations', () => { ... })` block:

1. **RES-01 — POST creates reservation:**
   ```javascript
   test.skip('api-post: POST /api/hotels/:slug/reservations returns 201 with reservation object', async ({ request }) => {
     const response = await request.post('/api/hotels/test-hotel/reservations', {
       data: { room_id: 'test-room-id', guest_name: 'Jonas', arrival_date: '2026-04-01', departure_date: '2026-04-03', nights: 2 }
     });
     expect(response.status()).toBe(201);
     const body = await response.json();
     expect(body.success).toBe(true);
     expect(body.reservation.guest_name).toBe('Jonas');
   });
   ```

2. **RES-01 — POST validation rejects missing fields:**
   ```javascript
   test.skip('api-post-validation: POST without guest_name returns 400', async ({ request }) => {
     const response = await request.post('/api/hotels/test-hotel/reservations', {
       data: { arrival_date: '2026-04-01', departure_date: '2026-04-03' }
     });
     expect(response.status()).toBe(400);
   });
   ```

3. **RES-03 — GET returns reservation list (admin):**
   ```javascript
   test.skip('api-get: GET /api/hotels/:slug/reservations returns list with session', async ({ request }) => {
     const response = await request.get('/api/hotels/test-hotel/reservations', {
       headers: { 'X-Session-ID': 'test-session' }
     });
     expect(response.status()).toBe(200);
     const body = await response.json();
     expect(body.success).toBe(true);
     expect(Array.isArray(body.reservations)).toBe(true);
   });
   ```

4. **RES-04 — PATCH status transitions:**
   ```javascript
   test.skip('api-patch-status: PATCH status to confirmed returns updated reservation', async ({ request }) => {
     const response = await request.patch('/api/hotels/test-hotel/reservations/test-id/status', {
       data: { status: 'confirmed' },
       headers: { 'X-Session-ID': 'test-session' }
     });
     expect(response.status()).toBe(200);
     const body = await response.json();
     expect(body.reservation.status).toBe('confirmed');
   });
   ```

5. **RES-01 — Form submit shows success message (E2E):**
   ```javascript
   test.skip('e2e-form: reservation form POST shows success message', async ({ page }) => {
     await page.goto('/hotel/test-hotel');
     // Fill form and submit — assert success message visible
     await expect(page.locator('.reservation-success')).toBeVisible();
   });
   ```

6. **RES-05 — WhatsApp link still visible as secondary CTA:**
   ```javascript
   test.skip('e2e-fallback: WhatsApp link visible as secondary CTA after form', async ({ page }) => {
     await page.goto('/hotel/test-hotel');
     await expect(page.locator('.btn-whatsapp')).toBeVisible();
   });
   ```

7. **RES-03 — Admin panel shows reservations section:**
   ```javascript
   test.skip('e2e-admin: admin page shows Rezervacijos section', async ({ page }) => {
     await page.goto('/hotel/test-hotel/admin');
     await expect(page.locator('#reservations-section')).toBeVisible();
   });
   ```

8. **RES-04 — Admin confirm button updates status:**
   ```javascript
   test.skip('e2e-admin-confirm: clicking Patvirtinti updates reservation status', async ({ page }) => {
     await page.goto('/hotel/test-hotel/admin');
     await page.click('.btn-confirm');
     await expect(page.locator('.status-confirmed')).toBeVisible();
   });
   ```

Use `import { test, expect } from '@playwright/test';` at the top. All tests use `test.skip()` so they compile and skip without server.
  </action>
  <verify>
    <automated>cd agent-network && npx playwright test hotel-reservations.spec.js --project=chromium 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `agent-network/tests/hotel-reservations.spec.js`
    - File contains `import { test, expect } from '@playwright/test'`
    - File contains at least 8 `test.skip(` calls
    - File contains `test.describe('Hotel Reservations'`
    - File contains strings: `api-post:`, `api-post-validation:`, `api-get:`, `api-patch-status:`, `e2e-form:`, `e2e-fallback:`, `e2e-admin:`, `e2e-admin-confirm:`
    - Running `npx playwright test hotel-reservations.spec.js --project=chromium` exits with 0 (all skipped, none failed)
  </acceptance_criteria>
  <done>8 test.skip() stubs exist covering all reservation behaviors; spec compiles and reports all skipped</done>
</task>

</tasks>

<verification>
- `cd agent-network && npx playwright test hotel-reservations.spec.js --project=chromium` exits 0 with all tests skipped
- `grep -c 'test.skip' agent-network/tests/hotel-reservations.spec.js` returns 8+
</verification>

<success_criteria>
hotel-reservations.spec.js exists with 8 test.skip() stubs that compile without error
</success_criteria>

<output>
After completion, create `.planning/phases/06-rezervacij-sistema-tikros-rezervacijos-db-savininkas-gauna-el-lai-k-ir-whatsapp-prane-im-apie-kiekvien-rezervacij/06-00-SUMMARY.md`
</output>
