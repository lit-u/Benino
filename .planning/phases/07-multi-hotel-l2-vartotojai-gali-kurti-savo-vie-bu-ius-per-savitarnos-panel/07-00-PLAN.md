---
phase: 07-multi-hotel
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - agent-network/tests/hotel-multi.spec.js
autonomous: true
requirements:
  - MH-01
  - MH-02
  - MH-03
  - MH-04
  - MH-05
  - MH-06
must_haves:
  truths:
    - "hotel-multi.spec.js exists with 6 test.skip() stubs"
    - "npx playwright test hotel-multi.spec.js compiles without error (all skipped)"
  artifacts:
    - path: "agent-network/tests/hotel-multi.spec.js"
      provides: "Playwright test stubs for MH-01 through MH-06"
      contains: "test.skip"
  key_links: []
---

<objective>
Create Playwright test stubs (test.skip) for all Phase 7 multi-hotel requirements before any implementation begins.

Purpose: Wave-0 pattern — executable spec compiles without server dependency, establishes test contract for MH-01 through MH-06.
Output: `agent-network/tests/hotel-multi.spec.js` with 6 skipped tests.
</objective>

<execution_context>
@C:/Users/Herba/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Herba/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-multi-hotel-l2-vartotojai-gali-kurti-savo-vie-bu-ius-per-savitarnos-panel/07-RESEARCH.md

<interfaces>
<!-- Existing test pattern from hotel.spec.js -->
```javascript
import { test, expect } from '@playwright/test';

test.describe('Hotel Mini-Website', () => {
  test.skip('ssr-title: /hotel/:slug returns HTML with hotel name in title', async ({ page }) => {
    // ...
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hotel-multi.spec.js with 6 test.skip stubs</name>
  <files>agent-network/tests/hotel-multi.spec.js</files>
  <read_first>
    - agent-network/tests/hotel.spec.js (existing test stub pattern to replicate)
    - agent-network/tests/hotel-reservations.spec.js (second example of Wave-0 pattern)
  </read_first>
  <action>
Create `agent-network/tests/hotel-multi.spec.js` following the exact pattern from `hotel.spec.js`.

Use `test.describe('Multi-Hotel Self-Service', () => { ... })` as the outer block.

Create these 6 `test.skip()` stubs:

1. **MH-01** `mine-api: GET /api/hotels/mine returns only caller hotels`
   - `await request.get('/api/hotels/mine', { headers: { 'X-Session-ID': 'test-session' } })`
   - Expect: `response.status() === 200`, `body.success === true`, `Array.isArray(body.hotels)`

2. **MH-02** `create-anon-blocked: POST /api/hotels without session returns 401`
   - `await request.post('/api/hotels', { data: { name: 'Test' } })` (no X-Session-ID header)
   - Expect: `response.status() === 401`

3. **MH-03** `create-success: POST /api/hotels by L2 user creates hotel and returns slug`
   - `await request.post('/api/hotels', { data: { name: 'Mano Namelis', city: 'Palanga' }, headers: { 'X-Session-ID': 'test-session' } })`
   - Expect: `response.status() === 201`, `body.slug` is truthy

4. **MH-04** `page-auth-gate: /my-hotels shows auth gate for unauthenticated user`
   - `await page.goto('/my-hotels')`
   - Expect: `page.locator('.auth-gate')` or text containing "Prisijunkite" is visible

5. **MH-05** `redirect-after-create: after hotel creation, browser navigates to /hotel/:slug/admin`
   - Comment: "Requires real L2 session — manual verification"
   - Minimal stub: `await page.goto('/my-hotels')` then assert page loaded

6. **MH-06** `soft-limit: 4th hotel creation returns 400`
   - Comment: "Requires 3 existing hotels for this owner — manual verification"
   - `await request.post('/api/hotels', { ... })` expect `response.status() === 400`

Each stub must have a comment stating which plan enables it (Plan 01 for API stubs, Plan 02 for page stubs).
  </action>
  <verify>
    <automated>cd D:/_PAL/benino/agent-network && npx playwright test hotel-multi.spec.js --project=chromium 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at agent-network/tests/hotel-multi.spec.js
    - Contains exactly 6 test.skip() calls
    - grep -c "test.skip" returns 6
    - npx playwright test hotel-multi.spec.js exits 0 (all skipped, none fail)
    - Each stub references its MH-XX requirement ID in a comment
  </acceptance_criteria>
  <done>hotel-multi.spec.js has 6 skipped test stubs covering MH-01 through MH-06; Playwright compiles and reports 6 skipped</done>
</task>

</tasks>

<verification>
```bash
cd agent-network && npx playwright test hotel-multi.spec.js --project=chromium
```
Expected: 6 skipped, 0 passed, 0 failed.
</verification>

<success_criteria>
- hotel-multi.spec.js exists with 6 test.skip stubs
- All stubs compile without syntax errors
- Playwright reports 6 skipped tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-multi-hotel-l2-vartotojai-gali-kurti-savo-vie-bu-ius-per-savitarnos-panel/07-00-SUMMARY.md`
</output>
